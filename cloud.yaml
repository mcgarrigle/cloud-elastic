---
version: "2"

guests:
  rocky:
    hostname: bench.local
    os: rocky8
    initialise: install
    args: 'console=ttyS0 ip=dhcp inst.text inst.ks=http://pi4.mac.wales/rocky8.ks'
    cores: 1
    memory: 2048
    disks:
      vda: 50G
    interfaces:
      eth0: bridge=br0
