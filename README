# cloud up; watch cloud ls
# cloud inv > inventory.yaml
# ansible-playbook -i inventory.yaml commission.yaml

// reload network

# cloud stop
# cloud up

# ansible-galaxy install elastic.elasticsearch,v7.14.0
# ansible-galaxy install fedelemantuano.kibana

# ansible-playbook -i inventory.yaml cluster.yaml
